mod back_of_house;
mod customer;
mod front_of_house;

fn deliver_order() {}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
